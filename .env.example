# Google OAuth credentials loaded by OpenMail at runtime.
# Never commit the real values. Copy this file to .env and place it next to
# the executable or run OpenMail from the project directory.
OPENMAIL_GMAIL_CLIENT_ID=your-google-client-id
OPENMAIL_GMAIL_CLIENT_SECRET=your-google-client-secret
