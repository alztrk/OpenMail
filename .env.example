# OAuth credentials are configured from Settings after OpenMail starts.
# They are stored in Windows Credential Manager and are never embedded in the binary.
# This file is intentionally empty of credential values.
