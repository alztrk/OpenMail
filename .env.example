# Google OAuth credentials used for local development builds.
# Never commit the real values. Copy this file to .env and load it before
# invoking the Tauri build command.
OPENMAIL_GMAIL_CLIENT_SECRET=your-google-client-secret
